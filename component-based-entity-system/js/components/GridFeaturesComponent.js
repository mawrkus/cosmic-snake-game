import { Component } from "./Component.js";

export class GridFeaturesComponent extends Component {
  constructor({ width, height, isDisabled, hasWarp, collision }) {
    super({ width, height, isDisabled, hasWarp, collision });
  }
}
